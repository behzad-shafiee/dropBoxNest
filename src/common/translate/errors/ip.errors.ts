import { IpgEnum } from "../enums/ipg.enum";
import { IpEnum } from "../enums/ip.enum";

export default {
  section: 'ip',
  values: {
    "IP_ADDRESS_ERROR": { key: 100000, value: IpEnum.IP_ADDRESS_ERROR  }
  },
};